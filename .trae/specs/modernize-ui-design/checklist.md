# Checklist（v2）

## 设计系统
- [x] 排版/间距/语义色/渐变/玻璃/阴影/z-index 令牌在 `:root` 与 `body.dark` 全量定义
- [x] 新增样式一律引用令牌，无新的硬编码颜色/间距

## 视觉语言
- [x] 导航毛玻璃 + 滚动加深生效；不支持 backdrop-filter 时纯色降级
- [x] 主按钮/关注/Tab 激活态/节标题强调条统一渐变，浅深两套一致
- [x] 首页 hero 横幅存在且移动端不溢出
- [x] 视频卡片：封面渐变遮罩 + 时长角标 + hover 浮起 + 标题 2 行截断
- [x] 详情页播放区暗色沉浸、操作按钮主次分明

## 深色模式
- [x] 导航栏切换按钮可用，localStorage 持久化，首访跟随系统偏好
- [x] 深色下全组件可读（弹幕层/骨架屏/toast/玻璃导航/hero 无漏网）

## 体验组件
- [x] 全站 0 处 `window.alert`，toast 自动消失、多条叠加、成功/错误分样式
- [x] 视频/文章/文件列表加载中显示骨架占位而非纯文本
- [x] 空状态组件覆盖：无关注/无搜索结果/无收藏/无历史
- [x] 导航/按钮/Tab/空状态无 emoji 图标，统一内联 SVG（离线可用）

## 导航统一
- [x] 全站页面导航结构一致（Logo + 居中搜索 + 右侧动作区 + 主题切换）——共 16 页
- [x] `updateNavigation()` 单一代码路径，旧 `.nav-links` 分支已删除
- [x] ≤560px 汉堡菜单可用，搜索可访问

## 响应式与可访问性
- [x] 560/900/1200 三档断点无横向滚动、无元素溢出
- [x] 移动端触控目标 ≥44px
- [x] `focus-visible` 焦点可见；正文对比度达 WCAG AA（muted 调至 4.65/4.87:1）
- [x] 全站动画在 `prefers-reduced-motion` 下降级（含弹幕）

## 信息层级
- [x] HTML 布局类内联样式清零（16 页 `style="` 为 0）
- [x] 节标题统一 `.section-header` 组件

## 回归
- [x] `npm test` 全部通过
- [x] 关注/弹幕/上传/搜索/通知/私信等既有功能无回归
